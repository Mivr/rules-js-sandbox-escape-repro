# Default: uses upstream rules_js — tests FAIL (demonstrating the bug)
# Use --config=with-fix to test with the native FS sandbox fix

# Point to the fork with the native FS sandbox fix.
# Update this path to your local checkout, or use --override_module directly:
#   bazel test //repro/... --override_module=aspect_rules_js=/path/to/your/fork
build:with-fix --override_module=aspect_rules_js=/Users/mihailvratchanski/IdeaProjects/rules_js
